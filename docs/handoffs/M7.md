# M7 Handoff — Cross-client proof

## Status

M7 is complete and was verified in production on 2026-09-22.

A novel fact introduced through Gremlin Chat / Model A was archived and
consolidated. A fresh OpenCode / Model B session that had never received the
Client A conversation retrieved the derived memory through authenticated MCP
and answered correctly. Server-side namespace authorization returned allowed
memory and excluded memory outside the principal's grants.

Exact production record identifiers, namespace details, internal endpoints,
credentials, and private memory content are intentionally not committed.

## Objective

Demonstrate that a fact introduced through Gremlin Chat / Model A can be
archived, consolidated into authorized derived memory, retrieved by a fresh
OpenCode / Model B session through MCP, and used to answer correctly without
giving Client B the original conversation.

## Delivered

- a dedicated OpenCode remote-MCP configuration under
  `clients/opencode/opencode.json`
- environment substitution for the MCP URL and Bearer credential
- OAuth auto-detection disabled for Gremlin's v0.1 API-key authentication
- OpenCode session sharing disabled
- explicit approval required for Gremlin MCP tools
- a `/gremlin-recall` command that forces evidence-backed, read-only recall
- a PostgreSQL integration test covering the complete Client A to Client B path
- a production acceptance runbook
- the agreed post-M7 roadmap
- successful production cross-client and authorization acceptance

## As-built map

| Concern | Location |
| --- | --- |
| OpenCode MCP configuration | `clients/opencode/opencode.json` |
| OpenCode recall command | `.opencode/commands/gremlin-recall.md` |
| Cross-client integration coverage | `apps/prime/test/integration/cross-client-continuity.integration.test.ts` |
| Production proof | `docs/runbooks/M7-cross-client-proof.md` |
| Post-M7 roadmap | `docs/roadmap.md` |

## Configuration contract

The OpenCode client requires:

- `GREMLIN_MCP_URL`: a reachable Streamable HTTP endpoint ending in `/mcp`
- `GREMLIN_MCP_API_KEY`: the one-time key for the dedicated OpenCode principal
- `OPENCODE_CONFIG=clients/opencode/opencode.json`

Secrets are intentionally absent from committed configuration.

## Acceptance

- [x] Introduce a novel fact through Gremlin Chat / Model A.
- [x] Verify the raw interaction and source principal.
- [x] Run consolidation.
- [x] Verify the derived memory and evidence link.
- [x] Start a fresh OpenCode session using a different model.
- [x] Verify OpenCode invokes Gremlin through MCP.
- [x] Answer correctly without receiving Client A's transcript.
- [x] Verify memory outside the principal's namespace grants remains absent.
- [x] Record sanitized production evidence.

## Production evidence

| Evidence | Result |
| --- | --- |
| Client A / Model A | Gremlin Chat / DeepSeek |
| Canonical source | Interaction persisted with authenticated `client:gremlin-chat` attribution |
| Consolidation | Production run succeeded |
| Derived memory | Memory, confidence, generator metadata, and source evidence link verified |
| Client B / Model B | OpenCode 1.18.32 / Claude Sonnet 4.6 through OpenRouter |
| MCP authentication | Dedicated OpenCode principal connected successfully |
| Authorized retrieval | Client B retrieved the M7 memory through authenticated MCP |
| Forbidden retrieval | Memory outside the OpenCode principal's grants was not returned |
| Final answer | Correct, using memory Client B had never previously seen |

## Verification

Repository verification completed before production acceptance:

```bash
pnpm typecheck
pnpm test
pnpm build
```

The deployed slice and full Client A to Client B path were then verified
against production PostgreSQL, pgvector, Gremlin Prime, Gremlin Chat, and
OpenCode.

## Boundaries

- No Prime API or persistence behavior changes were required by M7.
- OpenCode remains an external client; Gremlin does not launch or supervise it.
- Gremlin does not gain Docker socket access.
- M7 does not consolidate events or introduce background scheduling.
- M7 does not add memory mutation, OAuth, or broader authorization semantics.

## M8 entry point

M8 closes v0.1 against the complete Definition of Done: audit the locked
specification, implement the required canonical JSONL source export if still
missing, verify backup/restore and credential lifecycle operations, complete
release documentation, and tag v0.1.0.

# M8 Handoff — v0.1 audit and release

## Status

M8 is complete and was accepted in production on 2026-09-22.

M8 closes the locked v0.1 specification through auditable source export,
credential rotation, authorization failure verification, database recovery
testing, operational documentation, and final release acceptance. It does not
add new product scope.

## Delivered so far

- Definition-of-Done audit mapped to repository and production evidence
- repeatable-read JSONL export of canonical interactions and events
- bounded keyset pagination for exports without loading all history into memory
- owner-only export file permissions and overwrite refusal
- atomic API-key rotation for active principals
- negative namespace tests including prefix-sibling boundaries
- production export, rotation, backup, restore, and release runbook
- successful production acceptance covering export, credential rotation,
  namespace isolation, backup, independent restore, and provenance integrity

## As-built map

| Concern | Location |
| --- | --- |
| Definition-of-Done audit | `docs/audits/v0.1-definition-of-done.md` |
| Canonical export service | `apps/prime/src/source-export.ts` |
| Export command | `apps/prime/src/export-source.ts` |
| Credential rotation service | `apps/prime/src/principal-credentials.ts` |
| Rotation command | `apps/prime/src/rotate-principal-key.ts` |
| PostgreSQL export coverage | `apps/prime/test/integration/source-export.integration.test.ts` |
| Rotation coverage | `apps/prime/test/integration/principal-key-rotation.integration.test.ts` |
| Release operations | `docs/runbooks/M8-release-closure.md` |
| Production acceptance report | `docs/reports/M8-production-acceptance.md` |

## Acceptance

- [x] Run the complete PostgreSQL integration suite.
- [x] Export production canonical history and inspect representative records.
- [x] Rotate a production principal key and verify old/new authentication.
- [x] Reconfirm authorized and forbidden namespace behavior.
- [x] Create and validate a production PostgreSQL backup.
- [x] Restore the backup into a separate database and verify integrity.
- [x] Complete the Definition-of-Done audit.
- [x] Merge M8 into `main`.
- [ ] Tag the accepted `main` revision as `v0.1.0` after this report is merged.

## Production evidence

| Evidence | Result |
| --- | --- |
| Canonical export | PASS — 12 interactions and 1 event; JSONL, permissions, and overwrite protection verified |
| API-key rotation | PASS — replacement authenticated; previous key returned HTTP 401 |
| Namespace isolation | PASS — authorized recall succeeded; forbidden memory remained absent |
| Backup archive validation | PASS — custom archive validated successfully |
| Restore row-count comparison | PASS — all six recorded counts matched production |
| Restored provenance integrity | PASS — zero orphaned evidence links |
| Release tag | Pending |

Do not record credentials, private source content, internal endpoints, database
connection details, or backup locations in this document.

## Boundaries

- Source exports contain canonical interactions and events, not derived memory.
- The export format is versioned, but M8 does not add import tooling.
- Rotation supports one immediately active key per principal; grace periods and
  multi-key lifecycle management remain later operational work.
- Restore drills use a separate database and never overwrite production.
- Scheduled consolidation, event consolidation, memory lifecycle, and rebuild
  remain M9/M10 work.
